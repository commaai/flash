// Simplified platform detection - inline and direct
export const isWindows = navigator.userAgent.toLowerCase().includes('win')
export const isLinux = navigator.userAgent.toLowerCase().includes('linux')
